# Legacy video assets

The scheduled video runner no longer reads the static `index.json` queue. It
searches the official Pexels and Pixabay APIs for short MMA, kickboxing, Muay
Thai, and boxing footage, then publishes one unused result per slot.

The existing clips remain only as an archive of the previously published
queue. They are not eligible for new posts.
